# Brainbuster 1
# Find All customer names first & last, email Ids, customers of store #2

SELECT first_name, last_name, email, store_id
FROM customer
WHERE store_id = 2
;


# name of the actor with actor_id 50

select *
from actor
where actor_id = 50;